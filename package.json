{
  "name": "funkids",
  "private": true,
  "version": "1.0.0",
  "description": "Landing page FunKids con Angular y Cloudflare Pages Functions.",
  "scripts": {
    "start:frontend": "NG_CLI_ANALYTICS=false npm --prefix frontend run start",
    "start:functions": "npm --prefix frontend run functions:dev",
    "build:frontend": "NG_CLI_ANALYTICS=false npm --prefix frontend run build",
    "d1:migrate:local": "npm --prefix frontend run d1:migrate:local"
  }
}
